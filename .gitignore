# Rust build artifacts
/target/

# Cargo.lock for binaries (optional - some keep it, some don't)
# Cargo.lock

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Old git history backup
.git-old/

# Local backup bundles (created during experimentation / revert workflows)
/archive/revert_backup_*/

# Shader runtime dumps / scratch outputs
shader_dump_runtime.wgsl
combined_shaders.wgsl
shader_source_dump.wgsl

# Auto-snapshots (runtime output)
autosave_snapshot*.png

# Screenshots folder (user-generated captures)
screenshots/
